# Session 2026-08-15 — M1-11 Nachtest + Reihenfolge

## Done

### M1-11 Nachtest (kein Feature-Bau)

Gegen die laufende lokale Instanz (Beispiel GmbH, Kleinunternehmerregelung), HTTP über Caddy + Session.

**Bank (Abschn. 7)**

- Bankkonto Sparkasse war aus dem Funktionstest schon da.
- CSV de-DE (`Datum;Betrag;…`, Komma-Betrag, negativ = Ausgang) importiert: 2 Zeilen (Eingang 30,00 € „Rechnung R-0004“, Ausgang 25,50 € Hosting).
- Zweiter identischer Import: Lauf `2` gesamt / `0` neu / `2` Duplikat; Bewegungsbestand bleibt 2.
- Match-Vorschlag gegen offene Rechnung **R-0004**: Score 100 (Betrag = offener Rest, Nummer im Verwendungszweck, Datum nah). Bestätigung erzeugt Zahlung 30,00 €, Status Rechnung **bezahlt**, Bewegung **gematcht**.
- Journal vorher/nachher: **9** Zeilen — Zahlung erzeugt kein Journal.

**E-Rechnung Empfang (Abschn. 8)**

- Fixture `xrechnung-minimal.xml` → Original archiviert (Download bytegleich), Format XRechnung (UBL), Parse ok, Vorschau XR-2026-0042 / Muster Liefer GmbH / 119,00 €.
- Optional Beleg-Entwurf: Status `entwurf`, kein Journal, unter Kleinunternehmerregelung USt 0,00 / Netto = Brutto 119,00 €, Kategorie-Freitext „E-Rechnung“.
- Fixture `zugferd-cii-minimal.xml` → Original bytegleich, Format ZUGFeRD (CII), Parse ok, Vorschau ZF-2026-0007 / 59,50 €. Kein zweiter Beleg angelegt.
- Regelbesteuerung auf dieser Instanz nicht nachgetestet (Firma bleibt Kleinunternehmer). Mapping setzt dort USt aus dem Parse; unter Kleinunternehmer wird der Beleg ohne Vorsteuer vorbefüllt.

**Kein Blocker.** Alltag der beiden Keile trägt.

### Reihenfolge (mit kf)

1. Gemeinsame **Kategorien** für Belege und Kassenbuch (dieser Keil, vor M2).
2. **Multi-Firma dünn** (Firma anlegen + wechseln, eine Eigentümer:in).
3. **UStVA / ELSTER-XML light**.
4. Danach ZM, USt-IdNr., E-Rechnungs-Versand. Multi-User später.

Open Decisions unverändert nicht angefasst.

## Mängel aus dem Nachtest (nicht blockierend)

- **M1-15:** Erfolgs-`redirect()` in `importBankCsvAction` und `createBelegFromERechnungAction` sitzt im `try/catch` und wird als `error=NEXT_REDIRECT` angezeigt. Daten sind trotzdem korrekt geschrieben. Jobs-Actions haben bereits den Digest-Rethrow. Kein Fix in dieser Session (kein Blocker).

## Kategorien (nach dem Nachtest)

- ADR-0017: eine Liste für Belege und Kassenbuch; `kategorie` bleibt Text-Schnappschuss
- Collection `kategorien` (Migration `1730001300`), Client-Write gesperrt
- UI `/app/kategorien` unter Stammdaten; Select an Beleg und Kassenbuch
- Löschen nur ohne Verwendung; Umbenennen ändert nicht die Historie
- Verifiziert an der laufenden Instanz: Anlegen, case-insensitiver Duplikat-Schutz, unused Delete, Delete gesperrt bei 1 Beleg, Select inkl. historischem „E-Rechnung“
- 269 Unit-Tests + `tsc` grün

## Next step

Multi-Firma dünn (Firma anlegen + wechseln). Danach UStVA/ELSTER-XML light.
